'use client';

import styled from '@emotion/styled';

import { Color } from '@/styles/color';

export default function MainPage() {
  return (
    <S.Wrapper>
      contents
    </S.Wrapper>
  );
}

const S = {
  Wrapper: styled.div`

  `,
}
